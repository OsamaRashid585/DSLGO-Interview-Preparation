#include <iostream>
using namespace std;



int main(){
  string s = "acedecke";

    for (int i = 0; i < s.length(); i++)
    {
        for (int j = 0; j < s.length(); j++)
        {
            if ( s[i] == s[j] && i != j)
            {
               int n = i * j; // main
               string sn = to_string(n);
                cout << s[j];
                s[j] = sn[0]; 

            }
        }
           
    }
}
