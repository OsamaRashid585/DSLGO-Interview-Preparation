 private int[] arr = { 10, 12, 1 , 10, 57 };
    void Start()
    {
        for(int i =0; i < arr.Length; i++)
        {
            for(int j = 0; j<arr.Length; j++)
            {
                if(arr[i] == arr[j] && i != j)
                {
                    Debug.Log(arr[i]);
                }
            }
        }
    }
